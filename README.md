# cf_ai_ogooluwa_bot
Build an AI powered app on Cloudflare using Workers AI Llam 3.3
A demo AI chat app using:
- **Workers AI** (Llama 3.3 inference)
- **Durable Objects** (per-session memory)
- **Realtime** (WebSocket streaming)
- **Pages** (frontend UI)

## Run locally
```bash
npx wrangler dev
Deploy
npx wrangler deploy
